type Author {
  id: Int!
  firstName: String!
  lastName: String!
  posts: [Post!]!
}

type Post {
  id: Int!
  title: String!
  author: Author!
}

type Query {
  author(id: ID!): Author
  post(id: ID!): Post
  posts: [Post!]!
}
